export interface WhoAmI {
    id: number;
    name: string;
    icon: string;
    description: string;
    pictures?: string[];
}

export interface Training {
    id: number;
    name: string;
    icon: string;
    description: string;
    pictures?: string[];
}

export interface Contact {
    id: number;
    name: string;
    icon: string;
    description: string;
    pictures?: string[];
}